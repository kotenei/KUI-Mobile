import React, { StatelessComponent } from 'react';
import classnames from 'classnames';

const prefixCls = 'k-avatar';

const Avatar: StatelessComponent = props => {
  return <div />;
};

export default Avatar;
